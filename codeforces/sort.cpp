#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin>>s;
    int digitIndex[s.length()] = {false};
    for(int i = 0; i < s.length() - 1; i++){
        if(isdigit(s[i]){
            digitIndex[i] = true;
        }
    }

    sort(s.begin(), s.end());

    for(int i = 0; i < s.length() - 1; i++){

    }




    cout<<s;

    return 0;
}
